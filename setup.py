from setuptools import setup, find_packages

setup(
    name='eice-core',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Add any dependencies required for your module here
    ],
)